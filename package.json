{
  "name": "smartcharge_app",
  "version": "1.0.0",
  "scripts": {
    "build": "export PATH=\"$HOME/flutter/bin:$PATH\" && flutter pub get && flutter build web --release --no-tree-shake-icons"
  },
  "dependencies": {},
  "devDependencies": {}
}
